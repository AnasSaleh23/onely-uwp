<Page
    x:Class="Onely.PlayerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Onely"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <Style x:Key="PaneItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0"/>
        </Style>
    </Page.Resources>
    <RelativePanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" AllowDrop="True" DragOver="Playlist_DragOver" Drop="Playlist_Drop" KeyUp="Panel_KeyUp">
        <SplitView x:Name="FileCommand" RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignLeftWithPanel="True" RelativePanel.Above="Transport" IsPaneOpen="False" DisplayMode="CompactInline" CompactPaneLength="70" OpenPaneLength="70">
            <SplitView.Pane>
                <ListBox ItemContainerStyle="{StaticResource PaneItemStyle}">
                    <ListBoxItem>
                        <AppBarButton x:Name="OpenPlaylistButton" Icon="MusicInfo" Label="Playlists" IsEnabled="{x:Bind IsNotCurrentlyLoading, Mode=OneWay}"/>
                    </ListBoxItem>
                    <ListBoxItem>
                        <AppBarButton x:Name="AddFilesButton" Click="{x:Bind AddFiles}" Icon="OpenFile" Label="Add Files" IsEnabled="{x:Bind IsNotCurrentlyLoading, Mode=OneWay}"/>
                    </ListBoxItem>
                    <ListBoxItem>
                        <AppBarButton x:Name="AddFolderButton" Click="{x:Bind AddFolder}" Icon="Folder" Label="Add Folder" IsEnabled="{x:Bind IsNotCurrentlyLoading, Mode=OneWay}"/>
                    </ListBoxItem>
                    <ListBoxItem>
                        <AppBarButton x:Name="SavePlaylistButton" Click="{x:Bind OpenSaveDialog}" Icon="Save" Label="Save" IsEnabled="{x:Bind IsNotCurrentlyLoading, Mode=OneWay}"/>
                    </ListBoxItem>
                    <ListBoxItem>
                        <AppBarButton x:Name="ClearPlaylistButton" Click="{x:Bind ClearPlaylist}" Icon="Delete" Label="Clear" IsEnabled="{x:Bind IsNotCurrentlyLoading, Mode=OneWay}"/>
                    </ListBoxItem>
                </ListBox>
            </SplitView.Pane>
            <SplitView.Content>
                <ListView x:Name="PlaylistMenu" ScrollViewer.VerticalScrollMode="Auto" ItemsSource="{x:Bind SavedPlaylists, Mode=OneWay}">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:Playlist">
                            <TextBlock Text="{x:Bind Name, Mode=OneWay}"></TextBlock>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </SplitView.Content>
        </SplitView>
        <Grid x:Name="CoverImageContainer" RelativePanel.RightOf="FileCommand" RelativePanel.AlignRightWithPanel="True">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Image x:Name="CoverImage" Source="{x:Bind player.NowPlaying.Cover, Mode=OneWay}" MaxHeight="300" HorizontalAlignment="Center" Margin="0,0,0,24" Grid.Column="0"/>
        </Grid>
        <Grid x:Name="PlaylistLabels" RelativePanel.RightOf="FileCommand" RelativePanel.AlignRightWithPanel="True" RelativePanel.Below="CoverImageContainer">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Title" Padding="12,0" Grid.Column="0"/>
            <TextBlock Text="Album" Padding="12,0" Grid.Column="1"/>
            <TextBlock Text="Artist" Padding="12,0" Grid.Column="2"/>
        </Grid>
        <ListView x:Name="playlistUI" ScrollViewer.VerticalScrollMode="Auto" AllowDrop="True" CanDragItems="True" CanReorderItems="True" DoubleTapped="{x:Bind PlaylistItem_DoubleTapped}" ItemsSource="{x:Bind player.Playlist.Items, Mode=OneWay}" SelectedIndex="{x:Bind player.TargetIndex, Mode=TwoWay}" DragItemsStarting="{x:Bind Playlist_DragItemsStart}" DragItemsCompleted="{x:Bind Playlist_DragItemsCompleted}" RelativePanel.Below="PlaylistLabels" RelativePanel.RightOf="FileCommand" RelativePanel.AlignRightWithPanel="True" RelativePanel.Above="Transport">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:PlaylistItem">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="300"/>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="200"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{x:Bind Title}" Padding="12,0" Grid.Column="0"/>
                        <TextBlock Text="{x:Bind Album}" Padding="12,0" Grid.Column="1"/>
                        <TextBlock Text="{x:Bind Artist}" Padding="12,0" Grid.Column="2"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <CommandBar x:Name="Transport" VerticalAlignment="Center" RelativePanel.AlignBottomWithPanel="True" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True">
            <CommandBar.Content>
                <ProgressBar Minimum="0" Maximum="100" Value="{x:Bind player.PlayProgress, Mode=OneWay}" Width="300"/>
            </CommandBar.Content>
            <AppBarButton Click="{x:Bind RW}" Icon="Previous" Label="Rewind"/>
            <AppBarToggleButton Click="{x:Bind TogglePlayPause}" Icon="Play" IsChecked="{x:Bind player.IsPlaying, Mode=OneWay}" Label="Play"/>
            <AppBarButton Click="{x:Bind FF}" Icon="Next" Label="Fast Forward"/>
            <AppBarSeparator/>
            <AppBarToggleButton Icon="Shuffle" Click="{x:Bind ToggleShuffle}" Label="Shuffle"/>
            <AppBarButton Icon="{x:Bind ToggleRepeatIcon, Mode=OneWay}" Click="{x:Bind ToggleRepeatMode}" Label="Repeat Mode"/>
        </CommandBar>

        <MediaPlayerElement x:Name="mediaPlayerElement" AreTransportControlsEnabled="False" RelativePanel.Below="Transport"/>
    </RelativePanel>
    
</Page>


